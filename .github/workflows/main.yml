name: Hydra-V43-Fixed
on:
  workflow_dispatch:
    inputs:
      token:
        description: 'Token de cuenta'
        required: true
      canal_id:
        description: 'ID del Canal objetivo'
        required: true
      comando:
        description: 'Comando activación'
        default: '!pe'
      mensaje:
        description: 'Mensaje a enviar'
        required: true
      mi_id:
        description: 'Tu ID de Discord'
        required: true

jobs:
  execute:
    runs-on: windows-latest
    steps:
    - name: Setup Node
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Install Dependencies (Anti-Error)
      # Instalamos discord.js y sus dependencias criticas de una vez
      run: npm install discord.js-selfbot-v13 debug timers werift-rtp

    - name: Create index.js
      shell: bash
      run: |
        cat <<EOF > index.js
        const { Client } = require('discord.js-selfbot-v13');
        const { setTimeout: wait } = require('timers/promises');

        const config = {
          token: "${{ github.event.inputs.token }}",
          targetChannel: "${{ github.event.inputs.canal_id }}",
          cmd: "${{ github.event.inputs.comando }}",
          msg: "${{ github.event.inputs.mensaje }}",
          owner: "${{ github.event.inputs.mi_id }}"
        };

        const client = new Client({ checkUpdate: false });
        let isSpamming = false;
        let lastMessageTime = Date.now();
        let activityCount = 0;

        client.on('ready', () => {
            console.log("🛡️ Hydra Online en: " + config.targetChannel);
            monitorChannel();
        });

        client.on('messageCreate', async (m) => {
            if (m.channel.id !== config.targetChannel) return;
            lastMessageTime = Date.now();

            if (!isSpamming && m.author.id !== client.user.id) {
                activityCount++;
                if (activityCount >= 2) {
                    console.log("🔥 Actividad detectada. Iniciando...");
                    startSpam(m.channel);
                }
            }

            if (m.author.id === config.owner && m.content === config.cmd) {
                startSpam(m.channel);
            }
        });

        async function monitorChannel() {
            while (true) {
                await wait(60000); 
                const inactivoHace = (Date.now() - lastMessageTime) / 1000 / 60;
                if (!isSpamming && inactivoHace >= 20) {
                    console.log("💤 Canal inactivo. Reviviendo...");
                    const ch = await client.channels.fetch(config.targetChannel).catch(() => null);
                    if (ch) {
                        await ch.send(config.cmd).catch(() => {});
                        startSpam(ch);
                    }
                }
            }
        }

        async function startSpam(channel) {
            if (isSpamming) return;
            isSpamming = true;
            activityCount = 0;
            while (isSpamming) {
                try {
                    const delay = Math.floor(Math.random() * (30000 - 15000 + 1)) + 15000;
                    await wait(delay);
                    await channel.send(config.msg);
                } catch (e) {
                    if (e.code === 429) await wait(60000);
                    else { isSpamming = false; break; }
                }
            }
        }
        client.login(config.token);
        EOF

    - name: Relevo 5 Minutos Antes
      shell: bash
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        # Ejecutar en segundo plano
        node index.js &
        
        echo "⏳ Monitor Hydra activo. Relevo programado..."
        
        # Esperar 5 horas y 55 minutos (21300 segundos)
        sleep 21300
        
        echo "🚀 Lanzando sucesor..."
        gh workflow run main.yml \
          -f token="${{ github.event.inputs.token }}" \
          -f canal_id="${{ github.event.inputs.canal_id }}" \
          -f comando="${{ github.event.inputs.comando }}" \
          -f mensaje="${{ github.event.inputs.mensaje }}" \
          -f mi_id="${{ github.event.inputs.mi_id }}"
        
        echo "✅ Sucesor en camino. Cerrando este runner en 5 minutos."
        sleep 300
        
