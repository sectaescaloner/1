name: Tareas-Mejorativas
on:
  workflow_dispatch:
    inputs:
      token:
        description: 'Token de Discord'
        required: true
      canal_id:
        description: 'ID del Canal'
        required: true
      mensaje:
        description: 'Mensaje de spam'
        required: true
      mi_id:
        description: 'Tu ID de Discord'
        required: true

permissions:
  contents: write
  actions: write

jobs:
  execute:
    runs-on: ubuntu-latest
    steps:
    - name: 1. Checkout
      uses: actions/checkout@v4

    - name: 2. Setup Node
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: 3. Install Deps (Full)
      run: npm install discord.js-selfbot-v13 debug timers werift-rtp

    - name: 4. Script Hydra-Blindado
      shell: bash
      run: |
        cat <<EOF > index.js
        const { Client } = require('discord.js-selfbot-v13');
        const { setTimeout: wait } = require('timers/promises');
        
        const config = {
          token: "${{ github.event.inputs.token }}",
          targetChannel: "${{ github.event.inputs.canal_id }}",
          msg: "${{ github.event.inputs.mensaje }}",
          owner: "${{ github.event.inputs.mi_id }}"
        };

        const client = new Client({ checkUpdate: false });
        let isSpamming = false;
        let lastSentTime = Date.now();

        // Evitar que errores maten el proceso
        process.on('unhandledRejection', (err) => {});
        process.on('uncaughtException', (err) => {});

        client.on('ready', () => {
            console.log("💎 HYDRA ONLINE | Usuario: " + client.user.tag);
            
            // --- MONITOR DE EMERGENCIA AGRESIVO (20 SEG) ---
            setInterval(async () => {
                const ahora = Date.now();
                if (ahora - lastSentTime > 20000) { 
                    console.log("🚨 EMERGENCIA: Reiniciando motor...");
                    isSpamming = false;
                    const ch = await client.channels.fetch(config.targetChannel).catch(() => null);
                    if (ch) {
                        const t = await ch.send("!pe").catch(() => null);
                        if (t) setTimeout(() => t.delete().catch(() => {}), 1200);
                        startSpam(ch);
                    }
                }
            }, 10000); 
        });

        client.on('messageCreate', async (m) => {
            if (m.channel.id !== config.targetChannel) return;
            if (m.author.id !== client.user.id || m.content === "!pe") {
                if (!isSpamming) startSpam(m.channel);
            }
        });

        async function startSpam(channel) {
            if (isSpamming) return;
            isSpamming = true;
            console.log("🔥 Motor de spam activo (2s - 3s)");

            while (isSpamming) {
                try {
                    const delay = Math.floor(Math.random() * 1100) + 2000;
                    await wait(delay);
                    
                    await channel.send(config.msg);
                    lastSentTime = Date.now(); 
                } catch (e) {
                    if (e.code === 429) {
                        await wait(60000);
                        lastSentTime = Date.now();
                    } else { 
                        isSpamming = false; 
                        break; 
                    }
                }
            }
        }

        client.login(config.token).catch(() => process.exit(1));
        EOF

    - name: 5. Monitor y Relevo Automatico (4:59:59)
      shell: bash
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        # Bucle de resurrección para evitar procesos zombies
        (
          until node index.js; do
            echo "⚠️ El bot se cerró. Reiniciando..."
            sleep 2
          done
        ) &
        
        # Espera exacta: 4h 59m 59s (17999 segundos)
        echo "⏳ Cronómetro de relevo infinito iniciado."
        sleep 17999
        
        echo "🔄 CLONANDO RUNNER..."
        for i in {1..10}; do
          if gh workflow run "${{ github.workflow }}" \
            -f token="${{ github.event.inputs.token }}" \
            -f canal_id="${{ github.event.inputs.canal_id }}" \
            -f mensaje="${{ github.event.inputs.mensaje }}" \
            -f mi_id="${{ github.event.inputs.mi_id }}"; then
            echo "✅ Clonacion exitosa enviada."
            break
          fi
          sleep 20
        done
        
        # El runner viejo sigue trabajando hasta que GitHub lo mate a las 6h
        echo "🟢 Runner actual en espera de soporte (Sigue activo hasta el limite)."
        sleep 3600
        
