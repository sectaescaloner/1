name: 🐀 RATA_CHILE_WIDGET_FIXED
on: workflow_dispatch

jobs:
  gaming-heist:
    runs-on: windows-latest
    steps:
      - name: 🚀 1. HARDWARE SPOOF (RTX 5090)
        shell: powershell
        run: |
          $videoClass = "HKLM:\SYSTEM\CurrentControlSet\Control\Class\{4d36e968-e325-11ce-bfc1-08002be10318}"
          Get-ChildItem -Path $videoClass -ErrorAction SilentlyContinue | ForEach-Object {
              Set-ItemProperty -Path $_.PSPath -Name "DriverDesc" -Value "NVIDIA GeForce RTX 5090" -Force
          }
          Get-ChildItem -Path "HKLM:\HARDWARE\DESCRIPTION\System\CentralProcessor" | ForEach-Object {
              Set-ItemProperty -Path $_.PSPath -Name "ProcessorNameString" -Value "AMD Ryzen 9 7950X3D 16-Core Processor" -Force
          }

      - name: 🇨🇱 2. REGIÓN CHILE (SANTIAGO) - BYPASS
        shell: powershell
        run: |
          # Usamos try/catch para que si falla el GeoId, el script no se muera
          try {
              Set-WinSystemLocale -SystemLocale es-CL -ErrorAction SilentlyContinue
              Set-Culture es-CL -ErrorAction SilentlyContinue
              Set-TimeZone -Id "Pacific SA Standard Time" -ErrorAction SilentlyContinue
              # Este es el que suele dar error, lo aislamos:
              Set-WinHomeLocation -GeoId 47 -ErrorAction SilentlyContinue 
          } catch { 
              Write-Host "Ignorando error de GeoId, procediendo con el resto..." 
          }
          Write-Host "[OK] Configuración regional terminada."

      - name: 🎨 3. TASKBAR IZQUIERDA Y MODO OSCURO
        shell: powershell
        run: |
          $adv = "HKCU:\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced"
          if (!(Test-Path $adv)) { New-Item -Path $adv -Force }
          Set-ItemProperty -Path $adv -Name "TaskbarAl" -Value 0 -Force 
          Set-ItemProperty -Path $adv -Name "TaskbarDa" -Value 1 -Force 
          
          $theme = "HKCU:\Software\Microsoft\Windows\CurrentVersion\Themes\Personalize"
          if (!(Test-Path $theme)) { New-Item -Path $theme -Force }
          Set-ItemProperty -Path $theme -Name "AppsUseLightTheme" -Value 0 -Force
          Set-ItemProperty -Path $theme -Name "SystemUsesLightTheme" -Value 0 -Force

      - name: 🌦️ 4. MOTOR DE CLIMA Y RUSTDESK
        shell: powershell
        run: |
          # WebView2 (Silent)
          & curl.exe -L -o webview2.exe https://go.microsoft.com/fwlink/p/?LinkId=2124703
          Start-Process -FilePath ".\webview2.exe" -ArgumentList "/silent /install" -Wait
          
          # RustDesk
          & curl.exe -L -o rd.msi "https://github.com/rustdesk/rustdesk/releases/download/1.3.1/rustdesk-1.3.1-x86_64.msi"
          Start-Process msiexec.exe -ArgumentList "/i rd.msi /quiet /norestart" -Wait
          Start-Sleep -Seconds 5
          & "C:\Program Files\RustDesk\rustdesk.exe" --password Bullet123
          $id = & "C:\Program Files\RustDesk\rustdesk.exe" --get-id
          Write-Host "========================================"
          Write-Host "ID RUSTDESK: $id | PASS: Bullet123"
          Write-Host "========================================"

      - name: 📸 5. EVIDENCIA CATBOX
        shell: powershell
        run: |
          Add-Type -AssemblyName System.Windows.Forms, System.Drawing
          Start-Sleep -Seconds 15
          $screen = [System.Windows.Forms.Screen]::PrimaryScreen.Bounds
          $bitmap = New-Object System.Drawing.Bitmap($screen.Width, $screen.Height)
          $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
          $graphics.CopyFromScreen(0, 0, 0, 0, $bitmap.Size)
          $bitmap.Save("C:\Users\Public\desktop.png")
          # Subida a Catbox
          $link = & curl.exe -F "reqtype=fileupload" -F "fileToUpload=@C:\Users\Public\desktop.png" https://catbox.moe/user/api.php
          Write-Host "----------------------------------------"
          Write-Host "LINK DEL SCREENSHOT: $link"
          Write-Host "----------------------------------------"

      - name: 🔄 LOOP
        run: |
          while($true) { Start-Sleep 60 }

