name: RUST_PARINUX_PORTABLE
on: workflow_dispatch

jobs:
  deploy:
    runs-on: windows-latest
    steps:
      - name: 1. INFO RED
        shell: powershell
        run: |
          $ip = curl.exe -s https://ifconfig.me
          Write-Host "IP_DEL_RUNNER: $ip"
          Write-Host "TIP: Pon tu VPN en USA si no conecta."

      - name: 2. EJECUTAR RUSTDESK PORTABLE
        shell: powershell
        run: |
          $url = "https://github.com/rustdesk/rustdesk/releases/download/1.3.7/rustdesk-1.3.7-x86_64.exe"
          $exe = "$env:TEMP\rd.exe"
          curl.exe -L -o $exe $url
          
          Write-Host "Iniciando proceso portable..."
          # Lanzamos sin '--install' para que no se cuelgue
          Start-Process -FilePath $exe -ArgumentList "--password parinuxd"
          
          # Esperamos a que el proceso genere el ID
          Start-Sleep -Seconds 15

      - name: 3. CAPTURA GOFILE (ID AQUÍ)
        shell: powershell
        run: |
          Add-Type -AssemblyName System.Windows.Forms, System.Drawing
          $s = [System.Windows.Forms.Screen]::PrimaryScreen.Bounds
          $b = New-Object System.Drawing.Bitmap($s.Width, $s.Height)
          $g = [System.Drawing.Graphics]::FromImage($b)
          $g.CopyFromScreen(0, 0, 0, 0, $b.Size)
          $img = "$env:TEMP\id.png"
          $b.Save($img)
          
          $serv = (curl.exe -s https://api.gofile.io/servers | ConvertFrom-Json).data.servers[0].name
          $up = curl.exe -s -F "file=@$img" "https://$($serv).gofile.io/contents/uploadfile" | ConvertFrom-Json
          $link = $up.data.downloadPage
          Write-Host "LINK_CAPTURA: $link"
          Write-Host "PASS: parinuxd"

      - name: 4. MANTENER VIVO
        shell: powershell
        run: |
          while($true) { Start-Sleep 60 }
          
